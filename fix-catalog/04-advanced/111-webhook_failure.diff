# Aegis fix catalog #111 — Webhook signature verification fails
#
# Tier:       4 · Advanced
# Category:   webhook_failure (auto-diagnosed from the log below)
# Engine:     deterministic rule engine — no AI involved
#
# CI log:
#   | Error: webhook signature invalid — X-Hub-Signature-256 mismatch
#   | invalid webhook secret configured
#
# How it is solved:
#   MODIFY .github/workflows/ci.yml
#     - Added WEBHOOK_SECRET env var — webhook signature validation failed
#     - ensure the secret matches what is configured on the webhook provider
#
# Apply:      git apply 04-advanced/111-webhook_failure.diff
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -14,6 +14,7 @@
 env:
   CI: 'true'
   NODE_ENV: test
+  WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}  # ensure this matches your webhook configuration
 jobs:
   build:
     runs-on: ubuntu-latest
