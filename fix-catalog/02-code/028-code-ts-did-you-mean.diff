# Aegis fix catalog #028 — TypeScript misspelling — apply the compiler's suggestion (TS2551)
#
# Tier:       2 · Source code
# Category:   compilation_failure (auto-diagnosed from the log below)
# Engine:     deterministic rule engine — no AI involved
#
# CI log:
#   | src/users.ts(4,22): error TS2551: Property 'emial' does not exist on type 'User'. Did you mean 'email'?
#
# How it is solved:
#   MODIFY src/users.ts
#     - Applied the compiler's own "did you mean" suggestion: emial → email (line 4) — the compiler named the correct identifier
#     - only that token at the reported location was changed
#
# Apply:      git apply 02-code/028-code-ts-did-you-mean.diff
diff --git a/src/users.ts b/src/users.ts
--- a/src/users.ts
+++ b/src/users.ts
@@ -1,5 +1,5 @@
 interface User { name: string; email: string }
 
 export function contact(user: User) {
-  return `${user.name} <${user.emial}>`;
+  return `${user.name} <${user.email}>`;
 }
