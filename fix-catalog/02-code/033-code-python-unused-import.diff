# Aegis fix catalog #033 — flake8 / ruff F401 unused import
#
# Tier:       2 · Source code
# Category:   lint_failure
# Engine:     deterministic rule engine — no AI involved
#
# CI log:
#   | app/pricing.py:1:1: F401 'os' imported but unused
#   | app/pricing.py:2:1: F401 'typing.Optional' imported but unused
#   | Found 2 errors.
#
# How it is solved:
#   MODIFY app/pricing.py
#     - Removed unused Python imports 'os', 'typing.Optional' flagged by flake8/ruff F401 — lint gates in CI fail on unused imports
#
# Apply:      git apply 02-code/033-code-python-unused-import.diff
diff --git a/app/pricing.py b/app/pricing.py
--- a/app/pricing.py
+++ b/app/pricing.py
@@ -1,5 +1,5 @@
-import os, math
-from typing import Optional, List
+import math
+from typing import List
 
 
 def discount(price: float, pct: float) -> float:
