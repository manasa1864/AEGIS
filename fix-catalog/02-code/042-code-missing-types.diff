# Aegis fix catalog #042 — Installed package missing its `@types`
#
# Tier:       2 · Source code
# Category:   compilation_failure (auto-diagnosed from the log below)
# Engine:     deterministic rule engine — no AI involved
#
# CI log:
#   | src/server.ts(1,21): error TS7016: Could not find a declaration file for module 'express'. '/home/runner/work/app/app/node_modules/express/index.js' implicitly has an 'any' type.
#
# How it is solved:
#   MODIFY package.json
#     - Declared missing package @types/express in package.json — the code imports it but it was never added, so installs on a clean runner cannot resolve the import. Pinned to "latest": replace with the exact version and regenerate the lockfile before merging
#
# Apply:      git apply 02-code/042-code-missing-types.diff
diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@ -14,6 +14,7 @@
     "express": "^4.19.2"
   },
   "devDependencies": {
+    "@types/express": "latest",
     "typescript": "^5.4.0",
     "vitest": "^1.6.0"
   }
